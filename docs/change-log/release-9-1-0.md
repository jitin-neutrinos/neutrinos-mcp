# Features:

<https://documentation.neutrinos.com/articles/#!change-log/release-9-1-0>

### Features:

- `@electron/remote` and `electronlog` dependencies upgrade.
- Introduced jsonb column type for postgressql, mssql, mysql.
- - Node-pty issue with the electron version greater than 13.
- Env properties lookup for attributes window.
- [DM] Introduced autoGenerated UUID for column type string.
- [Migration] Move "OpenDialog" node from "pages" to "clientpages" in dependency.json of the app.
- [ConfigNodes] The design and generated code should not contain "users" array.

**Bug Fixes:**

- Cannot develop nodes seamlessly without reopening the studio to reflect code gen changes.
- URL keeps changing on `.npmrc` file.
- Error while creating a page. When creating a new app and clicking on that app immediately after creation few nodes are not loaded correctly i.e onInit, sql.
- Removed `googleMapKey` as a default environment key and added migration, if `Google map component` is not used in the app delete the key.
- `tabIconClass` attribute generates classes as comma(,) separated.
- `leftIconClick` and `rightIconClick` should be events.
- Migrated `IconBeforeLabelClass` and `IconAfterLabelClass` for Chips component
- Updated the `engine` object in bhivessd package.json and added migration.
- Added `@agm/core` to custom dependency.
- Navigating to view in this scenario fails.
- Installing the missing page ui component does not install plugins properly.
- URL keeps changing on .npmrc file.
- Changing `SSD_BASE_PATH` and clicking the `Open Swagger Docs` icon in SSD url redirection is wrong.
- Error while creating a page. When creating a new app and click on that app immediately after creation few nodes are not loaded correctly i.e onInit.
- The open `New Window` and `New Plugin Builder` does not work after renaming `Neutrinos Studio` in applications.
- Resolve node, Guardin routing, Guardout routing nodes can be copy pasted in the other CSD type services.
- Service designer search palette issue, accordion expand/collapse not working properly.
- Open dialog nodes should be available in CSD as well.
- UI breaks above the page ui (html editor) title.
- Creating the duplicate app name, the progress bar starts showing and has to close the studio.

e default topic template.
