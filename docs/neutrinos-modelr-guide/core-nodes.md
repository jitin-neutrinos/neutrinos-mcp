# Core Nodes

<https://documentation.neutrinos.com/articles/#!neutrinos-modelr-guide/core-nodes>

## Core Nodes

The nodes palette includes a default set of nodes that are the basic building blocks for creating flows. This page highlights the core set you should know about. Note that all nodes include documentation which you can see in the **Info** sidebar tab when you select a node.

- [Inject](/articles/neutrinos-modelr-guide/core-nodes/a/h3__1491769898)
- [Debug](/articles/neutrinos-modelr-guide/core-nodes/a/h3__1493730606)
- [Function](/articles/neutrinos-modelr-guide/core-nodes/a/h3__846004581)
- [Change](/articles/neutrinos-modelr-guide/core-nodes/a/h3__493109)
- [Switch](/articles/neutrinos-modelr-guide/core-nodes/a/h3__988362833)
- [Template](/articles/neutrinos-modelr-guide/core-nodes/a/h3__1369778639)

---

### The Inject Node

![](/resources/Storage/neutrinos-modelr-guide/inj.png)

The Inject node can be used to manually trigger a flow. It also can also be used to automatically trigger flows at regular intervals. The message sent by the Inject node can have its payload and topic properties set.

The payload can be set to a variety of different types:

- a flow or global context property value
- a String, number, boolean, Buffer or Object
- a Timestamp in milliseconds

---

### The Debug Node

![](/resources/Storage/neutrinos-modelr-guide/deb.png)

The Debug node can be used to display messages in the Debug sidebar within the editor. The sidebar provides a structured view of the messages it is sent, making it easier to explore the message. Alongside each message, the debug sidebar includes information about the time the message was received and which Debug node sent it. Clicking on the source node id will reveal that node within the workspace.

The button on the node can be used to enable or disable its output. It is recommended to disable or remove any Debug nodes that are not being used.

The node can also be configured to send all messages to the runtime log or to send short (32 characters) to the status text under the debug node.

---

### The Function Node

![](/resources/Storage/neutrinos-modelr-guide/func.png)

The Function node allows JavaScript code to be run against the messages that are passed through it.

For a complete guide for using the Function node, see [Writing Functions](/articles/neutrinos-modelr-guide/writing-functions).

---

### The Change Node

![](/resources/Storage/neutrinos-modelr-guide/change.png)

The Change node can be used to modify a message’s properties and set context properties without having to resort to a **Function **node. Each node can be configured with multiple operations that are applied in order.

The available operations are:

- **Set** - set a property. The value can be a variety of different types or can be taken from an existing message or context property.
- **Change** - search and replace parts of a message property.
- **Move** - move or rename a property.
- **Delete** - delete a property.

When setting a property, the value can also be the result of a JSONata expression. JSONata is a declarative query and transformation language for JSON data.

---

### The Switch Node

![](/resources/Storage/neutrinos-modelr-guide/swit.png)

The Switch node allows messages to be routed to different branches of a flow by evaluating a set of rules against each message. The node is configured with the property to test - which can be either a message property or a context property.

There are four types of rules:

- Value rules are evaluated against the configured property
- Sequence rules can be used on message sequences, such as those generated by the Split node
- A JSONata Expression can be provided that will be evaluated against the whole message and will match if the expression returns a true value.
- An Otherwise rule can be used to match if none of the preceding rules have matched.

The node will route a message to all outputs corresponding to matching rules. But it can also be configured to stop evaluating rules when it finds one that matches.

---

### The Template Node

![](/resources/Storage/neutrinos-modelr-guide/template.png)

The Template node can be used to generate text using a message’s properties to fill out a template. It uses the **Mustache** templating language to generate the result.

For example, a template of:

```json
This is the payload: {{payload}} !
```

Will replace {{payload}} with the value of the message’s payload property.

By default, Mustache will replace certain characters with their HTML escape codes. To stop that happening, you can use triple braces: {{{payload}}}.

Mustache supports simple loops on lists. For example, if msg.payload contains an array of names, such as: ["Nick", "Dave", "Claire"], the following template will create an HTML list of the names:

```html
<ul>{{#payload}}  <li>{{.}}</li>{{/payload}}</ul><ul>  <li>Nick</li>  <li>Dave</li>  <li>Claire</li></ul>
```

The node will set the configured message or context property with the result of the template. If the template generates valid JSON or YAML content, it can be configured to parse the result to the corresponding JavaScript Object.
