# Create an input or local flow variable

<https://documentation.neutrinos.com/articles/#!client-services-designer-8/service-designer-variables>

When you create a flow on the Client Services Designer, Neutrinos creates a flow level variable of object reference called bh to access input, local, error, and system properties of the flow. The properties include:

- bh.input: contains variables that are provided as arguments to the function created by the flow. They are not available outside the flow unless they are defined as Output variables or set to a page variable using the page variable node.

bh.local: contains variables that are private to the flow. They are additional variables that can be used inside the flow. They cannot be accessed outside the flow unless they are defined as Output variables or set to a page variable using the page variable node. The bh.error variable contains the error object after an exception occurs. These exceptions are caught by the **[Catch](/articles/client-services-designer-8/catch-node)** node.The bh.system variable contains all the variables and services that are pre-defined by the Neutrinos Platform. See [System-defined Properties](/articles/client-services-designer-8/system-defined-properties) to learn about these flow variables.The input and local variables are defined by the user, system variables are generated by theNeutrinos Platform, and the error variable is created when an exception occurs.




 ![Warning](/resources/Storage/client-services-designer-8/warning.png)


  You should not override the** bh **object. For example, you should not assign the following values to the** bh** object:bh = nullbh = undefinedbh = empty object

---

### Create an input or local flow variable

You can create any number of input or local variables in the **Start** and **Script** nodes of a flow.

#### Create input or local variables in the Start Node

To create **input** or **local** variables in the **Start **node, create a service or open an existing service, and perform the following steps:

1. Drag and drop a **Start** node.
2. To create an input variable, in the** Input Variables **field specify the input variables for the flow.
  - Enter an input key
  - Enter a value to be associated with the key. The value can be of type JSON, String, and number.
  - If you want to access the input variable outside the flow, assign it to be an output variable by toggling **Output ** to True. ![](/resources/Storage/client-services-designer-8/output_var.png)
  - Click the **Add** icon to add the input variable to the list.
3. To create a local variable, in the **Local Variables **field:
  1. Enter a local key
  2. Enter a value to be associated with the key. The value can be of type JSON, String, and number.
  3. If you want to access the local variable outside the flow, assign it to be an output variable by toggling **Output** to True. ![](/resources/Storage/client-services-designer-8/output_var.png)
  4. Click the **Add** icon to add the variable to the list.

![Information](/resources/Storage/client-services-designer-8/project-server-side-service-designer/info.png) If you want to set an input or local variable as an output variable (so that the variable can be accessed outside the flow), then you have to create the property only in the** Start** node and toggle the **Output** to **True**.

#### Create input or local variables in the Script node

To create **input** or **local** variables in the **Script **node, create a service or open an existing service, and perform the following steps:

1. Drag and drop a **Script** node.
2. To create an input or local variables, In the **Code Editor**, add the code to create one or more variables. If the variable already exists, its value will be updated, else a new variable will be created. For example:Copy CodeJavaScriptbh.input.modelrApiURL='http://localhost:24483/';
   bh.local.city='Goa';

| ![](/resources/Storage/client-services-designer-8/project-server-side-service-designer/info.png) | You cannot set input or local variables as output variables from the** Script** node. You have to use the **Start **node to do this operation. |
| --- | --- |
