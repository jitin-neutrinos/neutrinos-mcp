# Build a Local Server

<https://documentation.neutrinos.com/articles/#!tutorial-create-a-pwa/setup-a-server>

To test the app with a server, you can build a local server or run any HTTP server on the dist folder.

Perform the following steps to set up a local server:

1. Navigate to the **dist** folder inside the app. Enter the following command in the default terminal:
2. If you do not have an HTTP server installed locally, enter the following command to install it. If you already have an HTTP server, skip this step.
3. Run the HTTP server on a port that is available. Here, we are using port 8083.
4. Open a new terminal by clicking the **+** icon on the top right of the terminal window to start a dev server that is responsible for saving the **Subscription Object** and **Sending notification**. By default, the terminal holds the application folder. Navigate to the **server **folder.
5. Run the following command to start a server in Dev mode.
6. The server is set up and is now running.
